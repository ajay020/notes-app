<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".fragments.NoteListFragment">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerview"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="visible" />

    <!-- Empty message TextView -->

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:id="@+id/empty_place_holder"
        android:padding="26dp"
        android:visibility="gone">

        <ImageView
            android:layout_gravity="center"
            android:id="@+id/empty_image"
            android:layout_width="wrap_content"
            android:src="@drawable/add_notes_pana"
            android:contentDescription="Add note image"
            android:layout_height="wrap_content"/>

        <TextView
            android:id="@+id/emptyTextView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="@string/no_notes_available"
            android:textSize="18sp" />

    </LinearLayout>


</FrameLayout>